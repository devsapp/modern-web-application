
const SYSTEM_ROUTER = ['#/hubs', '#/workspace', '#/component-center', '#/rocket']
export function addOpenLinks() {
    const links = document.querySelectorAll('a[href]');
    const detectedLinks = document.querySelectorAll('.detected-link'); //monao 里面的标签<span>
    const linkListeners = [];

    
    return linkListeners;
}


export function getWbframe() {
    
}

export function openLinks(link) {
    
}

export function removeOpenLinks(links = []) {
    
}


export function openPathInFile(path) {
   
}